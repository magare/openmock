# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## Prototype feedback

- 2026-09-13: Camera Tools & Upload Viewport Non-Obstruction:
  - Camera tools (Rotate, Move, Roll, Fine, Reset View) and upload actions must stay outside the artwork viewport and never obstruct the mockup during interaction or live recording.
  - Desktop Side Rail: Dedicated in-flow `.camera-rail` (54px width) placed alongside the stage inside `.stage-workspace`, reserving physical layout space so rotated, panned, or zoomed devices cannot move underneath the controls. Supports left-handed layout symmetry via `flex-direction: row-reverse`.
  - Mobile Camera Toolbar: In-flow dedicated bar outside `.mockup-stage`, providing full touch access down to 320px with zero canvas obstruction and thumb-friendly left-handed reversal.
  - Persistent Media Actions & Clean Artwork Area: Floating canvas upload buttons (`.background-chip`, `.upload-toast`, `.media-badge`) removed from the artwork viewport. Upload and Replace are accessible via the persistent desktop TopBar (`.topbar-upload-btn`), the mobile dock utility bar (`.mobile-upload-btn`), the inspector SourceCard, and stage drag/drop and paste. Zero floating controls obstruct the 3D device.

- 2026-09-13: Usability Stage 4B — Mobile Discovery & Integrated Usability Verification:
  - Mobile Dock Ergonomics & Persistent Labels:
    - Dedicated In-Flow Flex Layout: Eliminated previous absolute position collision between mobile tabs and history controls at 320px width. `.mobile-dock` is an in-flow flex container (`flex: 0 0 50px`) holding `.mobile-utility-bar` (`flex: 0 0 auto`) and `.mobile-tabs` (`flex: 1 1 auto; min-width: 0; overflow-x: auto;`), guaranteeing 0px overlap across all viewports (100% responsive down to 320px).
    - Clear Media Action: High-contrast `.mobile-upload-btn` dynamically reflects media presence ("Upload" with upload icon when empty, "Replace" when media is loaded) alongside Undo and Redo actions.
    - Persistent Tool Labels: All 6 mobile tools (`Camera`, `Device`, `Scene`, `Blur`, `Effects`, `Settings`) display readable persistent labels and distinct icons with accessible `role="group"` and `aria-pressed` states. Touch targets maintain ≥36px height.
    - Left-Handed Layout Symmetry: Seamlessly reverses utility and tool docks via `flex-direction: row-reverse`, maintaining zero collisions and full control surface accessibility.
    - Extra Canvas Viewport Space: Floating control surface only renders when a tool is active (`:empty { display: none }`), returning 64px of vertical canvas space to mobile users.
  - Export Mode Tabs Keyboard Ergonomics & Description:
    - Arrow Key Navigation: Configured export tabs (`Still Image` vs `Video Animation`) support Left and Right arrow key traversal with roving focus.
    - Configured Dimensions Readout: Quick snapshot description dynamically reflects configured export resolution and format (`Instant still of current camera view at configured export size ([WIDTH] × [HEIGHT], [FORMAT]).`).
  - Integrated Verification & Decoded Downloads:
    - Full end-to-end journey verified: media upload > replace > device filter & select (Surface Laptop 15") > background preset ("Sunset") > camera pose adjustment > motion preset ("Low-angle pan up") > spacebar animation playback > project save & hydration reload > undo verification.
    - Real decoded assets: Quick Snapshot (JPEG, 1920 × 1080, 94.4 KB), Configured Still (PNG, 1920 × 1080, 769.8 KB), Configured Video Animation (WebM, 1280 × 720, 3.98s, 663.5 KB) with 28,394 differing pixels between frame 1 (0.1s) and frame 2 (0.7s) proving real motion.
    - Zero console errors across all journeys and viewports.
    - Verification: `scripts/qa/stage4b-verify.mjs` and screenshots in `evidence/qa/usability-stages/after/` (screenshots 70–80, `stage4b-video-frame-1.png`, `stage4b-video-frame-2.png`).

- 2026-09-13: Usability Stage 4A — Export Clarity & Unified Entry Point:
  - Single Export Entry Point: Removed ambiguous, unlabelled standalone camera icon (`.capture-button`) from TopBar. Unified all export and still capture actions under the single `EXPORT ▾` dropdown button (`.export-button`), expanding into an accessible dialog popover (`role="dialog" aria-modal="true" aria-label="Export and snapshot options"`).
  - Clear Distinction Between Quick Snapshot and Configured Export:
    - Dedicated Quick Action Card at the top of the popover: `QUICK ACTION` badge, `Quick snapshot` heading, clear explanatory description (`Instant full-resolution still of current camera view ([FORMAT]).`), and high-contrast `Download Snapshot` button with Camera icon.
    - Configured Export Section below a visual divider: tabbed interface (`Still Image` vs `Video Animation`) retaining all format, orientation, size, watermark, background transparency, quality, FPS, and motion blur options.
  - Busy State & Duplicate Prevention: TopBar export button dynamically displays live rendering progress (`EXPORTING 45%` / `EXPORTING…`); all export buttons (`Download Snapshot`, `Export Still Image`, `Export Video Animation`) are disabled (`disabled={exporting}`) while rendering to prevent concurrent or duplicate export requests. Video export displays an inline rendering progress bar.
  - Keyboard Focus & Accessibility: Opening the popover automatically focuses the primary snapshot button (`.export-quick-btn`); pressing Escape inside the popover or clicking the close button (`X`) immediately closes the popover and returns keyboard focus directly to the `EXPORT ▾` trigger button.
  - Viewport & Mobile Responsiveness: Popover positioned cleanly at desktop (`top: 50px; right: 6px; width: 336px;`) and mobile (`top: 46px; left: 8px; right: 8px; width: auto; max-height: calc(100dvh - 56px); overflow-y: auto;`), maintaining zero horizontal page overflow and full touch accessibility.
  - Real Download Verification: Verified real nonzero browser downloads for Quick Snapshot (JPEG, ~144 KB, `FF D8 FF`), Configured Still (PNG, ~763 KB, `89 50 4E 47`), and Configured Video (WebM, ~1.98 MB, `1A 45 DF A3`).
  - Verification: `scripts/qa/stage4a-verify.mjs` and screenshots in `evidence/qa/usability-stages/after/` (screenshots 60–66).

- 2026-09-13: Usability Stage 3 Repair — Layout Non-Overlap (1000px/1100px/1440px), Grid Sizing & Zero Nested Interactive Elements:
  - Grid & Flex Intrinsic Sizing at 1000px: Explicit `min-width: 0; max-width: 100%; width: 100%;` applied to `.workspace-grid`, `.main-column`, `.mockup-stage`, `.timeline-shell`, `.timeline-toolbar`, and `.timeline-content`, with `.timeline-spacer { flex: 1 1 0; min-width: 0; }`. Prevents CSS Grid intrinsic `min-content` blowout from stretching the stage and expanded timeline past `x = 696`.
  - Non-Overlap Verified: Canvas stage and timeline shell end at `x = 696`, maintaining a 12px separation before the fixed inspector at `x = 708` across 1000px, 1100px, and 1440px widths in both Light and Dark themes, with timeline minimized, expanded Simple, expanded Advanced, and presets popover open (`bottom: calc(100% + 8px)` floats cleanly without track or inspector collision).
  - Responsive Toolbar at `<= 1100px`: Compact toolbar rules hide subtitles (`.timeline-mode button small`) and mode hint (`.timeline-mode-hint`), use short labels (`AUTO`, `SHOT`, `REC`), compact 26px transport and action buttons, and a 48px zoom slider, fitting content in ~566px (well under the 684px column width) without requiring `overflow: hidden` on the shell.
  - Zero Nested Interactive Elements: `TrackRow` `.shot-segment` converted to a native `<button type="button" aria-pressed={active} ...>` container containing only its label; keyframe diamond markers (`.shot-keyframes-row`) are direct DOM siblings inside `.track-lane` positioned via `position: absolute; right: 8px; z-index: 2;`. `track-name-wrap` is a native `<button type="button">`. Zero nested interactive elements across the DOM.
  - Keyboard & Shortcut Safety: Native Enter and Space activation on mode switcher, transport buttons (Space toggles play/pause without activating canvas space-pan), track button, and keyframe diamond buttons (which stop key propagation).
  - Verification: `scripts/qa/stage3-repair-verify.mjs` and updated evidence in `evidence/qa/usability-stages/after/` (screenshots 40–58).

- 2026-09-13: Usability Stage 3 — Animation & Timeline Comprehension:
  - Mode Switcher & Contextual Explanation: Native `<button>` controls with accessible `role="group"` and `aria-pressed`, plain mode labels with secondary subtitles (`SIMPLE · Shots` vs `ADVANCED · Keyframes`), and an inline contextual hint badge (`Shots & Presets` vs `Keyframes & Recording`). Mode switching is strictly local UI view state, preserving all tracks, keyframes, duration, playhead, camera pose, interpolation, and undo history without data loss.
  - Static Timeline Guidance & Clear Next Step: For projects or newly added shots with `<= 1` keyframe, a compact inline status banner (`.timeline-static-guidance`) directs newcomers to make the camera move via direct actions (`Apply Motion Preset →`, `+ Add Shot`, or `Record Live`), without forced onboarding modals or auto-playing. Applying a preset immediately animates the camera and dismisses the static guidance.
  - High-Contrast Motion Presets Popover: Positioned above the timeline (`bottom: calc(100% + 8px); z-index: 60`), preventing bottom-edge clipping and track obstruction; solid card background with accessible dialog semantics, header, item descriptions, and Escape key close.
  - Transport, Playhead & Keyframe Clarity: High-contrast play/pause/back/loop transport buttons with explicit titles and shortcuts (`Play animation (Space)`), bold current playhead time (`00:00.0 / 00:04.0`), full unclipped track names (`Shot 1 ACTIVE 3s`), accessible keyframe diamonds with active double-ring styling, and accurate easing toggle (`Toggle easing (Linear / Ease in out)`) paired with a live keyframe badge (`Keyframe @ [time] · [easing]`).
  - Preserved Still Workflow: Initial timeline starts minimized (`timeline-mini`), keeping the canvas spacious for still mockup capture.
  - Verification: `evidence/qa/usability-stages/after/` (screenshots 40–49) and `scripts/qa/stage3-verify.mjs`.

- 2026-09-13: Usability Stage 2B — Scene Background Selection Simplification:
  - Single Background Expander: Inspector SCENE section has one dedicated Background expander with live summary (`Color · #...`, `Preset · [name]`, `Image · [name]`), active accent styling, and rotated chevron. Toggling the expander is local UI state and never mutates project state or undo history.
  - Flattened, Direct Choices: Eliminated nested `wide-select` dropdowns and secondary popovers (`backgroundPickerOpen` / `ChoicePopover`). Expanding Background directly exposes mode tabs (`Color`, `Preset`, `Image`) with accessible `role="group"` and native buttons with `aria-pressed`.
  - Bounded Direct Grids:
    - `Color`: hex input with native color picker swatch wrap and 8 quick color swatches with `aria-pressed`.
    - `Preset`: 11 presets in bounded `.preset-choice-grid` (`max-height: 220px; overflow-y: auto; overscroll-behavior: contain;`) displaying real swatches, active checkmarks, and accessible pressed states.
    - `Image`: 19 image templates in bounded `.image-choice-grid` (`max-height: 220px; overflow-y: auto; overscroll-behavior: contain;`) displaying real thumbnails, active checkmarks, and accessible pressed states.
  - Eliminated Duplicate Entry Points: Removed redundant `.background-image-row` and its stylesheet rules.
  - Preserved Semantics & History: Manual background changes correctly mark scene as `custom`, undo/redo faithfully steps through background selections and tab changes, scene presets (Dark room, Studio, Concrete) continue to apply their lighting and background environments, and keyboard focus reliably returns to the Background expander on Escape.
  - Verification: `evidence/qa/usability-stages/after/` (screenshots 30–39) and `scripts/qa/stage2b-verify.mjs`.

- 2026-09-13: Usability Stage 2A Repair — Source Card Vertical Sizing, Accessible Group Semantics & Focus Return:
  - Source Card empty state text clipping eliminated: `.source-card-empty` uses `min-height: 124px; height: auto;` (replacing fixed `height: 118px;`), with `.source-card-empty .source-card-text { flex: 0 0 auto; overflow: visible; padding-right: 0; width: 100%; gap: 3px; }` and explicit `line-height: 1.35;` on `strong`, `span`, and `small`. All instructions ("Upload screen", "Drop image or video, paste, or browse", format hints, "Browse") render with full letter height without vertical squishing or horizontal baseline clipping at both 1000px and 1440px widths across Light and Dark themes.
  - Accessible semantics: Device categories and catalog use native `<button>` controls with `aria-pressed` inside labeled `<div role="group" aria-label="Device categories">` and `<div role="group" aria-label="Device list">`, removing unsupported `role="tab"`/`tablist` and `role="radio"`/`radiogroup` patterns that lacked roving focus or arrow-key radio semantics.
  - Keyboard Focus Return: Pressing Escape inside the picker, clicking DONE, or selecting a device (via Enter, Space, or click) reliably returns keyboard focus directly to the Change device trigger (`.mockup-summary .change-button`) via `mockupChangeButtonRef` rather than losing focus to `document.body`.
  - Verification: `evidence/qa/usability-stages/after/` (screenshots 20–24) and `scripts/qa/stage2a-verify.mjs`.

- 2026-09-13: Usability Stage 2A — Inspector Ordering & Compact Device Category Filters:
  - Inspector sections sequence strictly: `SOURCE` → `MOCKUP` → `SCENE` → `CAMERA` → `EFFECTS`, matching the creative workflow (Media → Device → Scene & Background → Camera / Angle → Effects). Each section's independent open/closed toggle state in `panelSections` is preserved.
  - Device category filters in `MockupPicker` are dynamically derived from actual `mockupOptions` and `deviceGroup` data (39 devices total across 9 groups: `Stage`, `Phones`, `Tablets & readers`, `Laptops`, `Desktops`, `Watches`, `Handhelds`, `Web & TV`, `Spatial`). Filter tabs display live counts (`All (39)`, `Phones (13)`, etc.) and wrap to fit the 280px inspector.
  - The device grid is bounded inside `.mockup-picker-scroll` with `max-height: 280px; overflow-y: auto; overscroll-behavior: contain;` so expanding the catalog does not bury Scene, Camera, or Effects controls.
  - Active device identity remains continuously visible when its category is filtered out: Mockup summary card retains active device info, the containing category tab displays an active indicator dot, and a persistent notice banner (`Active: [Device] ([Category])`) with a `Show in All` reset link appears in the picker. Filters are strictly local view state, never mutating selected mockup or undo history.
  - Accessibility & Keyboard: Native `<button>` controls with `role="tab"`/`aria-selected` and `role="radio"`/`aria-checked`, clear "DONE" toggle and Escape key close actions, and automatic `scrollIntoView({ block: "nearest" })` on focus. Clicking `Change device →` from the Source guidance smoothly scrolls the Mockup section into view and moves focus into the picker without loss of keyboard context.
  - Verification: `evidence/qa/usability-stages/after/` (screenshots 20–25) and `scripts/qa/stage2a-verify.mjs`.

- 2026-09-13: Usability Stage 1 — Media/Device Workflow:
  - Source Card empty state uses plain labels ("Upload screen", "Drop image or video, paste, or browse", format hints, explicit "Browse" button, and accessible full-card click/keyboard trigger). Inspector section meta reflects live state (`ACTIVE` vs. `EMPTY`).
  - Filled state preserves visible, dedicated "Replace" and "Remove" buttons alongside an interactive preview thumbnail (`.source-preview-btn` with hover overlay). Never apply full-card invisible click overlays (`opacity: 0`) to `.source-card-filled > button` because it previously masked the remove button and intercepted all clicks.
  - Contextual next-step guidance (`.source-next-guidance`) appears below the filled source card, confirming media assignment to the active device and offering a direct action (`Change device →`) to open the mockup section and picker.
  - Mockup summary displays the device family/dimensions (`Phones · 1,206 × 2,622`), and its toggle button explicitly switches between `CHANGE` and `DONE` with active accent styling. In `MockupPicker`, the currently active item carries a visible checkmark icon (`.mockup-active-check`) and an "Active" badge to disambiguate the selected device in the 40-item catalog.
  - Verification: `evidence/qa/usability-stages/after/` and `scripts/qa/stage1-verify.mjs`.

- 2026-09-13: Device optimization must be selective, based on profiling, while preserving hardware detail. The 39-device profile selected Pixel 10 Pro/XL, Surface Laptop 15", Dell XPS 16, Nintendo Switch 2, Steam Deck, Galaxy Watch 8, Pixel Watch 4, and Apple Vision Pro. `src/deviceOptimization.js` batches compatible opaque static parts only on its explicit device allowlist; screens, transparency and independent live finish materials retain their behavior. Preserve the original part bounds when batching so normalization, camera framing and ground placement do not shift. The Pixel Pro derivative (`pixel-10-pro-optimized.glb`) reduces only `speakerGrills` and `alumPolished`, with locked borders, conservative error limits and high-precision Draco normals; the original manufacturer file remains the reference. Regenerate with `node scripts/prepare-pixel-model.mjs`. Watch straps keep the complete original tessellation and welded topology, with direct subdivision and a bounded four-shape CPU cache cloned per instance. Overlapping GLTF requests must share decoder workers with reference-counted leases, including React startup/remounts. Device switches must reset WebGL state, release detached asset resources and render targets, and dispose late-loading models without changing the current selection. Settled devices retain their canvas buffer; camera/controls, resize, image/HDR readiness and video frames must invalidate rendering so idle GPU work stays at zero without freezing live content. Regression evidence and measurements: `evidence/qa/device-optimization/`; browser probes: `scripts/qa/device-performance.mjs` and `scripts/qa/device-stability.mjs`.

- 2026-09-13: User asked to fix Nintendo Switch too and make the Dell laptop's missing trackpad visible. Nintendo Switch 2 now uses `createSwitch2` in `src/hardwareDevices.js`: 272 × 116 mm assembled silhouette, broad Joy-Con faces with flat inner edges and rounded outer shoulders, flush 7.9-inch 16:9 screen, layered sticks with blue/coral rings, separate directional buttons and correctly placed X/Y/A/B, +/−, Capture, Home and C buttons. Shoulder controls meet the body; the rear has a real open U-shaped kickstand, Nintendo's sourced wordmark, and top/bottom ports. Keep its fixed graphite colorway. Dell XPS 16 now has a visibly defined 148 × 90 mm glass trackpad with a subtle perimeter and tonal inset. This intentionally prioritizes the user's request for a readable trackpad over the previously invisible seamless treatment; do not remove its boundary again. The laptop display sits 0.35 mm beyond the lid face, and its reflection plane has another 0.1 mm of separation: smaller gaps caused visible depth-buffer stripes after normalization, especially front-on. References and provenance are in `evidence/qa/hardware-realism/REFERENCES.md`.

- 2026-09-12: User again flagged iPhone 16, Galaxy Tab S11 Ultra, Kindle Paperwhite, Galaxy Watch 8, Pixel Watch 4, Steam Deck, Surface Laptop 15", and Dell XPS 16 as inaccurate and toy-like. These eight now use measured rigs in `src/hardwareDevices.js`; this supersedes older procedural measurements/material decisions below. Preserve:
  - Millimetre dimensions and independent face radii: iPhone 71.6 × 147.6 × 7.8, Tab 208.5 × 326.3 × 5.1, Paperwhite 127.6 × 176.7 × 7.8, Deck 298 × 117, Surface 329 × 239, XPS 358.18 × 240.05. Thin shells use extruded face profiles; RoundedBoxGeometry clamps the face radius to half the thickness.
  - Flush display planes with normalized UVs replace raised screen blocks. The old iPhone 16 sampled world-coordinate UVs, clamping most of the image offscreen. `tests/hardware-devices.test.mjs` pins shell envelopes, corner clearance and full-face UVs. Keep the island/Tab notch black under bright lighting, and Paperwhite's screen matte with power/USB-C on the bottom.
  - Watches use curved solid straps with holes, keeper and fastening pin. Finish adaptive tessellation before bending, then weld vertices: early termination leaves triangular cracks. Galaxy has a lofted cushion case/twin keys; Pixel has a rounded aluminum case, domed black glass shoulder and ridged crown. Never revert to straight box straps.
  - Deck grips stay within its front outline and bulge behind it, with D-pad/ABXY outside the two sticks, trackpads below, horizontal speaker slots, triggers and four rear paddles. No hanging handles, raised display brick or controls buried inside grips.
  - Surface/XPS base and lid footprints match. Staggered keyboards have exactly one spacebar. Surface has silver keys, legends and an inset trackpad; its finish callback updates the case, keys and pad live. XPS stays graphite with close-spaced keys, capacitive function row, side speaker perforations and seamless glass palmrest. Transparent glass and labels do not cast rectangular shadows.
  - References: `evidence/qa/hardware-realism/REFERENCES.md`. In-app browser review: `/scripts/qa/hardware-review.html` (front/angled/back/side and finishes, with iPhone 17 as comparison). These are measured procedural rigs, not newly downloaded manufacturer meshes.

- 2026-09-12: Intuitive device movement (user report: dragging was very hard to predict). Decisions that must stick:
  - Canvas rotation uses screen-space quaternion deltas in `src/cameraInteraction.js`; horizontal and vertical strokes have the same visible direction across devices and existing poses, including back views, rolled devices, and vertical poles. Keep the legacy device-specific angle mapping only at the serialization/rendering boundary, so existing preset and saved rotation compositions survive. Never directly add pointer deltas to those legacy X/Y coordinates: phone yaw reversal and the X-to-roll contribution were the source of unexpected spinning.
  - Persistent canvas tools: Rotate, Move, Roll, Fine, Reset view. Plain scrolling now zooms (replaces the earlier scroll-to-rotate design); Shift slows any gesture fivefold, Space/middle-drag moves, Alt-drag rolls. The selected tool stays fixed for a stroke; changing Shift mid-stroke must not jump the pose. Mobile toolbar actions also select the Camera dock so Blur cannot silently override the chosen tool.
  - Movement uses 0.24 degrees per CSS pixel and no inertia. Full turns wrap without getting stuck at the manual sliders' ±360° boundary. Pan X/Y are fractions of the viewport half-size and use the live camera plane, so Move follows the pointer at any zoom or viewport size. Zoom changes the zoom value, not FOV.
  - Record one undo checkpoint per drag or wheel burst. Release pointer capture on up/cancel/lost capture/window blur; ignore additional pointers and toolbar interactions. Preserve the manually saved camera on mount: keyframe interpolation runs only when the playhead actually changes.
  - Reset view centers a front-facing pose and is undoable. Existing X/Y/Z sliders, camera presets, and recording remain available. Regression coverage: `tests/camera-interaction.test.mjs` and `scripts/qa/gesture-probe.mjs`.

- 2026-09-12: Move the transient live X/Y/Z/Zoom camera readout out of the canvas and into the centered top bar. It temporarily covers the viewport-ratio pill during camera gestures and must remain compact on mobile.

- 2026-09-12: Remove the Browser Window device from the mockup catalog; it is not needed. Its picker entry and dedicated DOM/three.js rendering code should remain absent.

- 2026-09-01 (iteration 7): iPhone 16 realism rework (user report: "make it look like real iPhone 16"). The procedural rig's camera system was a rounded-rectangle slab with the flash floating at the pill's outer corner — nothing like the real device. Decisions that must stick:
  - `createPhone` now only serves iPhone 16 (every other catalog phone has a manufacturer asset), so its body/backPanel/screen-stack build with `extrudedSlabGeometry` when `isIPhone16`: RoundedBoxGeometry clamps corner radius to half the DEPTH, which squared thin shells (~3.9mm effective vs the real ~11mm). The back panel and screen stack must keep a proportionally rounded profile or their corners poke through the rounder body.
  - All camera geometry is measured from Apple's official finish-select render (`evidence/qa/iphone-16-ref/finish-white.jpg`, ~5.72px/mm): rear pill 0.28w × 0.24h centered (+0.323w, +0.36h) — it hugs the top corner; lens rings 0.09-param (outer ≈ 0.2w) at ±0.062h pitch; tiny rear mic INSIDE the pill near its center-side edge (+0.238w, pill mid-height); small flash (r 0.0294) on the pill's side of the centerline (+0.11w) at pill mid-height. Dynamic Island is 0.27w × 0.04h at +0.454h with the front camera dot at its RIGHT end (+0.09w). Screen glass 0.94w × 0.985h (content ≈ 0.9w, thin even bezels). Earlier iteration-7 proportions (0.34×0.34 pill at +0.24w/+0.255h, left-side camera dot) were wrong — the official render is the source of truth.
  - `stadiumMesh` (extruded capsule with `facing` = −1 rear / +1 front) must be used for the pill and Dynamic Island, and it handles BOTH orientations — the horizontal branch exists because the vertical math degenerates when width > height (the island rendered as a giant blob before the fix). Side rails follow the 16 layout: Action button above volume rocker left, power plus a flush Camera Control lower right; body depth 0.115 (real 7.8mm at the rig's ~67mm/unit scale).
  - Identity cues and materials: the back carries an Apple logo (`addAppleLogo` — canvas-drawn apple silhouette alphaMap on a polished insert, centered, drawn pre-flipped with `flipY = false` because Chrome rejects FLIP_Y on that upload path). Lens stack per ring: bright aluminum ring (registered in `extraFinishables` so the finish picker recolors it), then glossy near-black glass (0x04060a, roughness 0.05, low metalness — bright HDR environments wash out metallic darks) whose cap must sit PROUD of the solid ring cylinder's face, plus a small offset bright inner element. The 16's back glass is matte frosted (roughness 0.46, low clearcoat) — gloss read as plastic.
  - Body WIDTH and buttons (user-flagged, round 2): the body slimmed to 1.03×2.2 (ratio ≈ 0.47, matching the official render's ~0.465 measured footprint; the pill/mic/flash/island fractions were rebalanced — pill 0.29w at +0.33w, mic +0.247w, flash +0.114w, island 0.28w — so their approved absolute sizes stayed put). Buttons are near-flush slivers: ~1mm proud (x = ±(width/2 ± 0.001), w = 0.03), thin blades (z-depth 0.28×depth), at measured centers/heights — action +0.281h (h 0.04h), volume pair +0.191h/+0.097h (h 0.064/0.066h), power +0.145h (h 0.113h), Camera Control nearly flush at −0.1h (h 0.055h). The old layout protruded ~2.4mm blades that read as fins. `sideButton` grew a 5th `zDepth` param (default preserves the old look for galaxy/pixel/generic).
  - Camera-pose QA gotcha: the live stage camera comes from the active track's keyframes (`cameraAtTime`), not the stored `project.camera` — on load the app snaps the pose to the keyframe/default preset, so probe scripts must inject keyframes carrying the pose under test (see `scripts/qa/iphone16-check.mjs`). Evidence: `evidence/qa/iphone-16-fix/` (back/front/angled/detail, console clean). Gates: build, test:editor 17/17, test:sites 4/4.

- 2026-09-01 (iteration 6): Final realism pass for the expanded catalog (user directive: keep iterating until every new device looks like the physical product). Decisions that must stick:
  - Manufacturer geometry now replaces the generic shells for Galaxy S26/S26+ (`galaxy-s26*.glb`, native display meshes `M1_Display_Activearea` / `M2_Display_Activearea`), Pixel 10 (`pixel-10.glb` from Google's own interactive viewer), iPhone Air (`iphone-air.usdz`, screen `tppzCQmnlKcHIpP`), iPad A16 (`ipad-a16.usdz`, screen `YnQokSaETxDhiat`), and both iPad Air sizes (`ipad-air.usdz`, screen `duUosXawNfgWFdH`). The Air asset's keyboard and pencil subtrees stay removed. iPhone Air is authored edge-on and requires the generic `shapeRotateY: Math.PI / 2` preparation option before `slab` facing correction. Pixel 10 Pro XL intentionally reuses Google's exact Pro chassis because the visible proportions and camera system are shared; OpenMock still binds the XL screen aspect.
  - Galaxy Watch 8 and Pixel Watch 4 remain procedural because no usable manufacturer mesh is published. Watch8 must keep its thin graphite cushion case, narrow circular bezel, twin side buttons, and centered connector; Pixel must keep the polished round housing, edgeless shallow-hemisphere glass, crown, and integrated continuous band. `openmockOpacityScale` prevents reflection controls from turning the domed glass into an opaque mirror.
  - Physical dimensions now drive the remaining procedural silhouettes: Tab S11 Ultra is 326.3:208.5 and very thin with separate rear camera rings/S Pen; Switch 2 is 272:116 with a dominant 7.9-inch screen and wide rear kickstand; Steam Deck is 298:117 with larger grips, correct 16:10 display, trackpads, speakers, and controls; Surface 15 and XPS 16 use their deeper real footprints, while XPS keeps its capacitive function strip, zero-lattice key spacing, and low-contrast seamless trackpad. iPhone 16's cameras must stay vertically stacked in one pill.
  - Final live-browser QA: all 40 catalog entries reached renderer status `ready`, all fit values were finite, front/angled/back sweeps of the new hardware were visually reviewed against official Samsung/Google references, and browser console warnings/errors were empty. Gates: `npm run build`, `test:editor` 17/17, `test:sites` 4/4, and `git diff --check` pass.

- 2026-09-01 (iteration 5): Galaxy Watch 8 / Pixel Watch 4 realism rework (user report: round watches looked weird). The straight-box straps and flat puck case were the problem; no manufacturer GLB exists on Samsung's watch pages (checked), so the rigs were rebuilt: the band is now a single partial open cylinder (axis along X, `side: DoubleSide`) wrapped around an implied wrist — its arc meets the case just behind the 12 o'clock edge and recedes behind it at 6, exactly like product photography. Case is tapered (different top/bottom cylinder radii) and thinner; Galaxy keeps slim bezel ring + crown/ridges + side button + speaker slot, Pixel is bezel-less flush glass with a single crown and narrower band. Geometry math for the wrap: attach point at theta = gap/2 → cylinder center y/z = attachY − R·sin(gap/2), attachZ − R·cos(gap/2). Evidence: `evidence/qa/goal-sweep/galaxy-watch-8.png`, `pixel-watch-4.png`. Gates: test:editor 17/17, build + test:sites 4/4.

- 2026-09-01 (iteration 4): Realism pass on the new devices (user directive: every device must look like the actual hardware). Decisions that must stick:
  - Real manufacturer assets fetched and wired for four new devices, discovered via the compare/product pages the existing assets came from: Apple Watch Series 11 (`watch-series-11.usdz`, screen mesh `WJGSzuMjppLwYru`), Apple Watch SE 3 (`watch-se-3.usdz`, `YzRtkPWMxJWtiye`), Galaxy Z Fold 8 (`galaxy-z-fold8.glb` from Samsung's compare viewer, `Main_Display`), Galaxy Z Flip 8 (`galaxy-z-flip8.glb`, `Main_Display`). Both Samsung GLBs are posed OPEN (inner display forward, square Sub_Display on the back); the Flip's screen UVs are stored vertically mirrored, fixed with a new `flipV` (and generic `flipU`/`yawFlip`) exact-asset option — texture rotation alone cannot fix a mirrored plane.
  - New exact-asset helper: `scripts/inspect/browser-glb-inspect.mjs` prints mesh names/sizes/positions for Draco GLBs by loading them in a real browser via the dev server's decoder (Node GLTFLoader can't do Draco workers; gltf-transform CLI cannot decompress).
  - Fixed-colorway procedural rigs where no manufacturer asset exists — Switch 2 and Steam Deck (true black hardware, joycon accent lines, sticks/diamond buttons/dpads/trackpads/angled Deck grips), Kindle Paperwhite (charcoal body, black front, power key, no page buttons), TV 65" (black panel + edge feet regardless of finish). These return `finishables: []` + `body: null` so the finish picker deliberately leaves the colorway alone. Laptop rig overhauled for Surface/XPS (keyboard well + 6-row key matrix with function row and wide spacebar, centered trackpad, thin bezels, rubber feet) — shared with the procedural MacBook fallback. Browser window gained a tab strip + URL row; Galaxy/Pixel round watches split into two styles (slim black bezel ring + lugs vs bezel-less flush glass, no lugs).
  - Bug class to remember: `for (const [i, [dx, dy]] of [[0,[1,0]]…].entries())` double-wraps pre-indexed pairs — `dy` becomes an array, `position.set` goes NaN, `visibleMeshBounds`/`refitStage` produce `fit: NaN` and the device silently renders invisible (status still "ready"). Hit the Switch/Deck buttons and the browser tab loop; both rewritten to plain pair iteration.
  - Regression-checked against the older path: iMac/Studio/XDR (createDisplay refactor), MacBook Neo (USDZ), iPhone 17 Pro. Evidence: `evidence/qa/goal-sweep/` (40/40 `ready`, zero console errors). Gates: test:editor 17/17, build + test:sites 4/4.

- 2026-09-01 (iteration 3): Catalog grew 17 → 40 devices (user request: "add the suggested devices plus iPhone 16"). Decisions that must stick:
  - Name→family mapping is centralized in `deviceArchetype()` / `isAndroidMockup()` / `deviceGroup()` in `src/editorState.js`; both renderers (three.js stage, DOM fallback) and the camera framing/`fitTarget` branch on the archetype, never on ad-hoc `includes` checks. Order inside `deviceArchetype` matters (watch/foldable/Tab before generic phone/tablet matches) and is pinned by tests.
  - New devices without manufacturer assets reuse the closest family GLB/USDZ (iPhone 16/17 Air → iPhone 17 GLB, Galaxy S26/S26+ → Ultra GLB, Pixel 10/XL → Pixel GLB, iPad (A16)/Air 11/13 → iPad Pro USDZ with re-cropped screens, Apple Watch Series 11/SE 3 → Watch Ultra USDZ). Accepted compromise: the Apple watches share the Ultra case shape (only Apple watch model available); Galaxy Watch 8 / Pixel Watch 4 use a new procedural round-case rig instead.
  - New procedural rigs in `ThreeStage.jsx`: browser window (chrome bar + traffic dots + URL pill), foldables (crease seam + punch hole + Samsung camera layout), TV (black bezel regardless of finish, display-stand variant), handhelds (Switch 2 thin slab + flush rails vs Deck with thick angled grips), e-reader (matte body + page keys), plus Galaxy Tab variant in `createTablet`. DOM fallback anatomy for all of these lives in `editor-enhancements.css`; `StageDevice` (dead duplicate of the fallback renderer) was deleted.
  - Mockup picker is grouped by `deviceGroup()` section labels (STAGE / WEB & TV / PHONES / …); Kindle sits after iPad Pro so "Tablets & readers" stays one contiguous section, and group Fragments key on group index because a label can legitimately appear only once now (a duplicate-key console error was found and fixed via capture).
  - `scripts/qa/device-sweep.mjs` derives its default device list from `mockupOptions` (no more hardcoded 17), so sweeps always cover the full catalog. Evidence: `evidence/qa/goal-sweep/` (40/40 `ready`, zero console errors), `evidence/qa/mockup-picker/picker-groups.png`. Gates: test:editor 17/17, build + test:sites 4/4.

- 2026-09-01 (iteration 2): Centered framing + live axis HUD. Decisions that must stick:
  - `refitStage` now centers the device with even breathing room: the fit margin is 0.85 of the reference half-frame and the pose's editorial vertical offsets were removed — the projected box of the rotated model is centered via an 8-corner projection nudge (`halfHWorldLive` correction), so perspective (near-side magnification) no longer makes the device sit high. Zoom/FOV still bypass the reference frustum and keep their effect; pan remains a deliberate user offset. `canvas[data-openmock-fit]` exposes the applied fit for QA probes.
  - Bounds used for normalization, the cached `modelHalf`, and ground placement all go through `visibleMeshBounds` (skips invisible nodes / fully transparent or shadow-material meshes). Discovered while debugging: `extentY` from half-extent projections is ALREADY a half-height — the old `extentY / 2` denominator doubled the fit and silently disabled the clamp (device rendered ~106% and clipped) until fixed.
  - Live axis HUD: while a stage gesture drives the camera, a pill at the stage top shows `X ±°· Y ±°· Z ±°· ZOOM ×` (`.stage-axis-hud`, written imperatively via `axisHudRef` so 60Hz gestures don't re-render the tree; auto-hides ~900ms after the last event). First gesture also dismisses the onboarding hint chip (hint state lifted to `App` as `stageHintSeen`).
  - `scripts/qa/device-sweep.mjs` port aligned to 5199 (was 5173); full 17-device sweep re-run clean against the new bounds path (`evidence/qa/ui-goal-r3-sweep/`).
  - Evidence: `evidence/qa/ui-goal-final/`, `hud-during-scroll.png` in `ui-goal-r3`; `scripts/qa/gesture-probe.mjs` also asserts the fit engages at the default pose and the HUD appears during a scroll gesture. Gates re-run: test:editor 14/14, build + test:sites 4/4.

- 2026-09-01: Editor UI clarity + framing round (goal: spacious canvas, device centered and fully visible, controls clearly communicated, X/Y/Z grouped with ±360° live values). Decisions that must stick:
  - Adaptive device framing in `ThreeStage.jsx`: after each pose update, `refitStage` scales the stage root so the rotated model's projected extent plus the preset's vertical offset fits a reference frustum (24° FOV at the family's base camera distance, 0.97 margin, live aspect for width). Live Zoom/FOV deliberately do NOT feed the reference frustum, so those sliders keep their framing effect (zooming past the fit is the requested crop); rotation-driven growth and short/narrow stages auto-shrink instead of cropping. The iPhone 17 vertical offset went -0.26 → -0.18 (less bottom bias, more centered). Aspect changes re-fit per frame via `runtime.lastFitAspect`.
  - Stage hint chip speaks in plain phrases with key chips: "Drag to orbit · Scroll to rotate · [⇧] tilt · [⌥] roll · [⌘]/[Ctrl] zoom"; mobile gets "Drag to rotate the device". Visible ~14s or until first stage pointerdown. Same wording in the Shortcuts modal ("Orbit device / Rotate device / Tilt — Y axis / Roll — Z axis / Zoom") and tour step 2.
  - Inspector CAMERA groups its sliders: ROTATION card (X · Turn, Y · Tilt, Z · Roll; ±360° badge; note "Canvas: drag orbits · scroll rotates · ⇧ tilt · ⌥ roll") + FRAMING card (FOV, Zoom, Pan X/Y). Live outputs and keyframe diamonds unchanged.
  - Mobile camera dock gained X/Y/Z rotation sliders (±360°, live °) beside the move/tilt/zoom radios and FOV dial — axis control is no longer desktop-only.
  - Stage wheel handling moved to a native `passive: false` listener (React's onWheel is passive, so preventDefault was ignored and Cmd/Ctrl scroll also zoomed the browser page). `scripts/qa/gesture-probe.mjs` now covers drag-orbit / scroll-rotate / ⇧ / ⌥ / ⌘ zoom / slider-set plus a canvas-edge alpha probe that proves the device stays inside the stage; zero console errors (favicon added to `index.html`).
  - Polish: --ink-3 label contrast raised in both themes (#6e6e69 light, #9da0a2 dark), range thumbs get accent hover rings, timeline toolbar swaps to short labels (AUTO/REC) at ≤1100px.
  - Evidence: `evidence/qa/ui-goal-baseline/` (before), `evidence/qa/ui-goal-r1|ui-goal-r2/` (after, incl. `zoom-camera-panel.png`, `17b-mobile-settled.png`). `npm run test:editor` 14/14, `npm run build` + `npm run test:sites` 4/4.

- 2026-09-01: Canvas rotation feedback: scrolling should rotate the device through a full 360° with less input, and the X/Y/Z camera axes must remain directly adjustable. Plain scroll now orbits, Shift/Option scroll target tilt/roll, and Cmd/Ctrl scroll keeps zoom available; camera axes use ±360°.

- 2026-09-01: Full UI/UX overhaul (11 rounds, user directive: iterate until the UI is easy to use and looks good; check the UI after every round). Decisions that must stick:
  - Single design-token stylesheet: `src/styles.css` is the only chrome skin (tokens for panel/card/ink/line/accent, light + dark). `src/editor-enhancements.css` now contains ONLY functional CSS (3D canvas + fallback device anatomy, stage/effect/DOF layers, keyframe chips, track-kind tints). The old duplicate "compact chrome" override skin was removed — it absolutely positioned the ratio button (overlap bug), painted slider tracks transparent, and hard-coded light colors that broke dark mode.
  - All range inputs share one custom track (4px, orange fill via `--fill` set by the component) with a 14px white thumb. Anything overriding a range input needs `input[type="range"].class` specificity, because the global rule is (0,1,1).
  - Top bar: menu · brand · INFO/TEMPLATES/HELP — spacer — centered ratio — spacer — Undo/Redo · divider · Save · theme · capture · EXPORT. No `order` hacks (they caused the overlap). "FREE CAPTURES" chip and duplicate "FREE" button were removed as noise. EXPORT is visible on mobile too.
  - Inspector IA: five icon sections — SOURCE, SCENE, MOCKUP, CAMERA (moved out of MOCKUP), EFFECTS (own section, count meta, inline effect picker `.effect-picker-inline`).
  - Sliders show units (°, ×, %); effect amount rows are labeled "Amount".
  - Timeline toolbar order: mode · transport · time · spacer · PRESETS/AUTO-MOTION/RECORD-or-ADD-SHOT · divider · LENGTH · guides · zoom · add-track · minimize. The dead grip button is gone; LENGTH now actually sets `timeline.duration` (parses m:ss, 1–180s). The scrubber is a tick-marked ruler row under the toolbar. Timeline popovers close on Escape; all popovers close on outside pointerdown.
  - Stage: "DRAG TO ORBIT · SCROLL TO ROTATE · ⇧ Y · ⌥ Z · ⌘/CTRL ZOOM" hint chip until first pointerdown, grab/grabbing cursor, `touch-action:none`, device-loading pill, drag-over drop highlight, media badge top-left. Source chip and caption stay desktop-hidden (matches the live editor).
  - Regression tooling: `scripts/qa/ui-capture.mjs <outDir> [baseline|full]` captures the state matrix (18 shots: both themes, pickers, popovers, export, timeline modes, presets, camera, effects, preferences, 1100/900px, mobile) into `evidence/qa/`. Functional sweep (scene/preset/effect/undo/play/length) verified via console-error-free Playwright run. `npm run build`, `npm run test:sites`, `npm run test:editor` pass.
  - Found + fixed a pre-existing crash: clicking EXPORT threw `setTab is not defined` (prop renamed without updating the JSX) and white-screened the whole app.
- 2026-08-31: Full functionality audit (every control exercised in-browser). Fixes and decisions:
  - Timeline playback and scrubbing now interpolate the camera along the active track's keyframes (`cameraAtTime` + `easeValue` in `src/editorState.js`, playhead effect in `App.jsx`). The effect is keyed on the playhead only so manual drags and keyframe-chip selections keep their pose. Video export follows the same keyframed path instead of the old sinusoidal drift, honors the selected size (1080P no longer clamps to 720P), maps Quality to `videoBitsPerSecond`, and implements Motion Blur as per-frame alpha blending (Off/Low/Med/High).
  - Media upload/paste/drop were silently discarded: the history dedupe compared `getSerializableProject()` output, which strips blob-URL media, so an upload serialized identical to the previous state. The dedupe now compares full project JSON; persistence still uses `getSerializableProject`. Superseded blob URLs stay alive until unmount so undoing back to an earlier shot still renders its media.
  - Fast double-clicks on the tour's Next could push `tourStep` past 8 and the `tourContent[step-1]` destructuring crashed React and unmounted the app. Step is clamped and `TourModal` guards out-of-range steps.
  - All 12 effects now have a visible stage implementation. Previously-dead ones (Glass Border, Depth, Sharpen, Fish Eye, Screen Fade, Ghost, Liquid Glass) render as per-effect child spans inside `.three-effects-overlay` (see `editor-enhancements.css`), avoiding pseudo-element collisions. Grain, Pixel Grid, Chromatic Abb., and Screen Fade also apply in canvas exports; Fish Eye / Depth / Ghost / Liquid Glass / Sharpen are stage-preview-only (canvas export keeps its flat re-render).
  - The mobile Blur dock is wired: `.stage-dof-layer` overlays the stage with a masked `backdrop-filter` driven by blur strength/size/falloff/mode (radial/directional/tilt) plus a subtle bokeh texture. Stage-preview only — exports do not include DOF.
  - The "Add keyframe at playhead" diamond buttons on camera sliders were dead (`preventDefault` only); they now pin a camera keyframe via `addCameraKeyframe`. Lighting sliders no longer show one (lighting isn't keyframed). Keyframe chips and lane dots show a selected state.
  - Regression: `npm run test:editor` covers `scenePatch`, `easeValue`, and `cameraAtTime`; `scripts/qa/scene-probe.mjs` passes; `npm run build` + `npm run test:sites` pass.
- 2026-08-31: Scene options must actually apply their environment (user report: scene options did nothing). Scene presets are lighting + background presets: `scenePatch(value)` in `src/editorState.js` maps `dark-room` to Dark Rim lighting + near-black color backdrop + contact shadow, `concrete` to the Metal preset gradient + contact shadow, `studio` to Studio Soft + Whisp image; `custom` keeps the user's controls. `selectScene` in `src/App.jsx` applies the patch atomically, and any manual lighting/background edit (picker choices, light rotation, contact shadow, bg blur, mobile scene dials, templates) flips the scene back to `custom` so the summary stays truthful. The BG BLUR slider is now live: the stage background renders in a dedicated `.stage-background-layer` (blurred DOM layer behind the canvas) and `exporter.js` applies the same blur to exports. Lighting presets also scale `scene.environmentIntensity` in `ThreeStage.jsx` so Dark Rim reads dark on the PBR device models. Regression tooling: `scripts/qa/scene-probe.mjs`, captures in `evidence/qa/scene-probe/`.
- 2026-08-31: Device rotation range widened from ±180° to ±225° (180 + 45 on each end) per user request — applies to the X/Y/Z Axis sliders in `CameraControls` and the stage drag clamps in `handleStagePointerMove` (`src/App.jsx`). Regression captures: `evidence/qa/rotation-225-x.png`, `evidence/qa/rotation-minus225-x.png`, `evidence/qa/rotation-minus225-y.png`, `evidence/qa/rotation-drag-past180.png`.
- 2026-08-30: User reports that the reconstruction does not match the reference editor. Treat visual fidelity to the captured source screenshots as the priority for future changes.
- 2026-08-30: User wants every selectable device mockup to look realistic, with the supplied iPhone 17 GLB treated as the quality bar for geometry, materials, camera framing, and hardware detail across the catalog.
- 2026-08-31: Device realism decisions (user directive: every device must look like the physical hardware):
  - Every catalog mockup loads its manufacturer model: Apple USDZ AR assets for the iPhone 17 Pro family, Watch Ultra 3, iPads, MacBooks, iMac, Studio Display, Vision Pro, and XDR; Samsung/Google viewer GLBs for Galaxy S26 Ultra and Pixel 10 Pro. The supplied iPhone 17 GLB stays the baseline for the base iPhone. Procedural rigs remain only as WebGL-failure fallbacks and for `Flat`.
  - Manufacturer USDZ materials must have `aoMap` stripped in `prepareExactDevice` — the converted geometry lacks the second UV set those textures reference, and leaving them produces structured noise on rails, decks, and stands.
  - Media screens bind per device, chosen by what renders correctly: native mesh UVs with `flipY` kept true when the baked screenshot renders upright (Watch, iPad Pro, with per-device quarter-turn or v-flip corrections), hidden-mesh overlay quads when UV rewrite is unreliable (iPhone Pro family, Pixel, iPad mini), and the world-space UV rewrite for the displays that tolerate it (MacBooks need `screenFlipV`, iMac/Studio/XDR do not).
  - Accessories that ship inside AR assets are removed per config: Apple Pencil from both iPads, Magic Keyboard and Magic Mouse from iMac 24.
  - The iPhone 17 Pro USDZ packs eight overlapping color-variant phones; `isolateIPhoneProVariant` must delete every other candidate slab, not just non-top-level scene children.
  - XDR Display gets a stainless stand via explicit `standMeshes` material swap because its stand is merged into assembly meshes; its foot stays dark (merged geometry, accepted).
  - Regression evidence and per-device captures live in `evidence/qa/goal-sweep-final2/` (angled sweep + montage), `evidence/qa/orientation-front/` (front-on), and `evidence/qa/rear/` (camera decks). Probe tooling: `scripts/qa/device-sweep.mjs`, `scripts/qa/orientation-front.mjs`, `scripts/qa/rear-sweep.mjs`, `scripts/inspect/material-probe.mjs`, `scripts/inspect/slab-probe.mjs`, `scripts/qa/montage.mjs`.
